<UserControl x:Class="ShutDown.Views.Patterns.NewPatternControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ShutDown.Views.Patterns"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:NewPatternViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="Silver"
                             Opacity="0.95" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock Style="{StaticResource H1}">New Pattern</TextBlock>
            <TextBlock Grid.Row="1">
                New pattern will be saved with settings chosen on main screen. (
                <Run Text="{Binding NewPatternDescription.Value}"></Run>
                )
            </TextBlock>
            <TextBlock Grid.Row="3">Please type new pattern name:</TextBlock>
            <TextBox Grid.Row="4"
                     Margin="0, 5"
                     Width="200"
                     HorizontalAlignment="Left"
                     Text="{Binding NewPatternName.Value}" />
        </Grid>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="1"
                    VerticalAlignment="Center"
                    Width="215">
            <Button Content="Save new pattern"
                    Style="{StaticResource BtnSelect}"
                    Width="120"
                    Command="{Binding SavePatternCommand}" />
            <Button Content="Cancel"
                    Style="{StaticResource BtnSelect}"
                    Margin="15, 0, 0, 0"
                    Command="{Binding CancelNewPatternCommand}" />
        </StackPanel>

    </Grid>
</UserControl>
