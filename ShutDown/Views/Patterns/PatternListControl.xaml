<UserControl x:Class="ShutDown.Views.Patterns.PatternListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShutDown.Views.Patterns"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:PatternListViewModel/>
    </UserControl.DataContext>

    <Grid Margin="0, 10, 0, 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Predefined settings"
               Grid.ColumnSpan="2"
               Style="{StaticResource H1}" />

        <Button Grid.Row="1"
                Grid.Column="0"
            Style="{StaticResource BtnHead}"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="+"
            Margin="7, 0, 0, 0"
            Command="{Binding ShowNewPatternViewCommand}" />

        <ItemsControl Grid.Row="1" Grid.Column="1" Margin="5, 0, 0, 0"
              ItemsSource="{Binding Patterns.Value}">
            <ItemsControl.Template>
                <ControlTemplate>
                    <WrapPanel  Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" 
                            FlowDirection="LeftToRight" IsItemsHost="true">
                    </WrapPanel>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal"
                            Margin="2">
                        <Button Content="{Binding Name}" 
                            ToolTip="{Binding Description}"
                            Padding="2, 2, 4, 2"
                            BorderThickness="0"
                            CommandParameter="{Binding Id}"
                            Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:PatternListControl}}, Path=DataContext.StartFromPatternCommand}"
                            />
                        <Button Content="X"
                            Margin="-1, 0, 0, 0"
                            ToolTip="Delete pattern"
                            Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:PatternListControl}}, Path=DataContext.DeletePatternCommand}"
                            CommandParameter="{Binding Id}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
